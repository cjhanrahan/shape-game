import { MaterialType } from '@/graphics/materials'

export const Config = {
    antialiasing: true,
    materialType: MaterialType.GRADIENT,
    minAnswerDelta: 0.05,
    minVolume: 10,
    maxAnswerDelta: 0.15,
    maxVolume: 100,
    plane: false,
}