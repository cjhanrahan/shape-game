import { MaterialType } from '@/graphics/materials'

export const Config = {
    antialiasing: true,
    materialType: MaterialType.GRADIENT,
    minAnswerDelta: 0.05,
    minVolume: 10,
    maxAnswerDelta: 0.25,
    maxVolume: 100,
    plane: false,
}